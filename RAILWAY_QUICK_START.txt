╔═══════════════════════════════════════════════════════════════╗
║           🚂 miiwiichat → Railway Deployment                  ║
║                  Quick Start Guide                            ║
╚═══════════════════════════════════════════════════════════════╝

📦 WHAT'S INCLUDED:
  ✅ Full deployment configuration files
  ✅ Step-by-step deployment guide
  ✅ Helper deployment script
  ✅ Production-ready setup

📁 IMPORTANT FILES:
  📄 DEPLOYMENT_STEPS.md    - Detailed deployment guide
  📄 README.md              - Full documentation
  📄 deploy.sh              - Helper script to push to GitHub
  📄 railway.toml           - Railway configuration
  📄 .gitignore             - Git ignore rules

🚀 DEPLOYMENT IN 3 STEPS:

  STEP 1: Push to GitHub
  ─────────────────────────
  $ ./deploy.sh
  
  (Or manually):
  $ git init
  $ git add .
  $ git commit -m "Deploy miiwiichat"
  $ git remote add origin https://github.com/YOUR_USERNAME/miiwiichat.git
  $ git push -u origin main

  STEP 2: Deploy on Railway
  ─────────────────────────
  1. Go to https://railway.app
  2. New Project → Deploy from GitHub repo
  3. Select your miiwiichat repo
  4. Add MongoDB (+ New → Database → MongoDB)
  5. Copy MongoDB URL

  STEP 3: Configure Services
  ─────────────────────────
  Backend Service:
    • Settings → Root Directory: backend
    • Settings → Start Command: uvicorn server:app --host 0.0.0.0 --port $PORT
    • Variables:
      MONGO_URL=<your-mongodb-url>
      DB_NAME=miiwiichat
      CORS_ORIGINS=*
      JWT_SECRET=change-this-secret
      PORT=8001
    • Generate Domain

  Frontend Service (+ New → GitHub Repo → Same repo):
    • Settings → Root Directory: frontend
    • Settings → Build: yarn install && yarn build
    • Settings → Start: npx serve -s build -p $PORT
    • Variables:
      REACT_APP_BACKEND_URL=https://your-backend.railway.app
      PORT=3000
    • Generate Domain

🎯 AFTER DEPLOYMENT:

  ✅ Test your app at the Railway URL
  ✅ Register a user with 8-digit number
  ✅ Make yourself admin in MongoDB:
     db.users.updateOne(
       { email: "your@email.com" },
       { $set: { is_admin: true } }
     )

💰 COST:
  Free Tier: $5/month credit
  Production: ~$10-15/month
  
  Includes:
  • MongoDB database
  • 2 services (frontend + backend)
  • HTTPS + Custom domains
  • WebSocket support

📚 NEED HELP?
  • Read DEPLOYMENT_STEPS.md for detailed guide
  • Check README.md for full documentation
  • Railway docs: https://docs.railway.app
  • Railway Discord: https://discord.gg/railway

═══════════════════════════════════════════════════════════════

🎉 Your Discord clone will be live in under 10 minutes!

Start here: https://railway.app

═══════════════════════════════════════════════════════════════
