{
  "name": "miiwiichat",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && yarn install && yarn build && cd ../backend && pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8001}",
    "build": "cd frontend && yarn install && yarn build",
    "backend": "cd backend && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8001}"
  }
}