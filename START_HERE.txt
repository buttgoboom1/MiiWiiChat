╔═══════════════════════════════════════════════════════════════╗
║                    🚀 START HERE                              ║
║              miiwiichat Railway Deployment                    ║
╚═══════════════════════════════════════════════════════════════╝

📦 YOU HAVE EVERYTHING YOU NEED!

This zip contains a complete Discord clone with:
  ✅ Real-time messaging
  ✅ Video/Voice calling  
  ✅ Admin dashboard
  ✅ 8-digit user numbers
  ✅ Google OAuth + JWT auth

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 DEPLOY TO RAILWAY IN 5 MINUTES:

STEP 1: Extract & Push to GitHub
─────────────────────────────────
1. Extract this zip file
2. Create a new GitHub repository at https://github.com/new
3. Name it "miiwiichat" (or whatever you like)
4. In the extracted folder, run:

   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/miiwiichat.git
   git branch -M main
   git push -u origin main

   OR just run: ./deploy.sh

STEP 2: Deploy on Railway
─────────────────────────────────
1. Go to https://railway.app and sign in
2. Click "New Project"
3. Select "Deploy from GitHub repo"
4. Choose your miiwiichat repository
5. Wait for initial deployment (will fail - that's OK!)

STEP 3: Add MongoDB
─────────────────────────────────
1. In your Railway project, click "+ New"
2. Select "Database" → "Add MongoDB"
3. Copy the MONGO_URL (looks like: mongodb://...)

STEP 4: Configure Backend
─────────────────────────────────
1. Click on your backend service
2. Go to "Settings" tab:
   • Root Directory: backend
   • Start Command: uvicorn server:app --host 0.0.0.0 --port $PORT

3. Go to "Variables" tab, add these:
   MONGO_URL=<paste your MongoDB URL>
   DB_NAME=miiwiichat
   CORS_ORIGINS=*
   JWT_SECRET=my-super-secret-key-12345
   PORT=8001

4. Settings → Networking → "Generate Domain"
5. Copy your backend URL (e.g., backend-xyz.railway.app)

STEP 5: Add Frontend Service
─────────────────────────────────
1. Click "+ New" → "GitHub Repo" → Select same repo
2. Click on this new service
3. Settings:
   • Root Directory: frontend
   • Build Command: yarn install && yarn build
   • Start Command: npx serve -s build -p $PORT

4. Variables:
   REACT_APP_BACKEND_URL=https://your-backend-url.railway.app
   PORT=3000

5. Generate Domain

STEP 6: Test Your App! 🎉
─────────────────────────────────
Visit your frontend Railway URL and:
1. Register with 8-digit user number
2. Create servers and channels
3. Send messages
4. Try video/voice calls

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION:

├─ RAILWAY_QUICK_START.txt  ← Quick reference
├─ DEPLOYMENT_STEPS.md      ← Detailed guide
├─ README.md                ← Full documentation
└─ deploy.sh                ← Helper script

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 MAKE YOURSELF ADMIN:

After registering, connect to Railway MongoDB:
1. Railway → MongoDB service → Connect
2. Run this query:

   use miiwiichat
   db.users.updateOne(
     { email: "your@email.com" },
     { $set: { is_admin: true } }
   )

Now access /admin in your app!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💰 COST: $5-15/month (includes $5 free credit)

❓ NEED HELP? Read DEPLOYMENT_STEPS.md

🎊 YOU'RE ALL SET! Happy deploying!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
